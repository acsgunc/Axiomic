{
  "name": "axiomic-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Serverless data-fetch proxy for Axiomic (Cloudflare Worker).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
