# Base URL of the serverless data-fetch proxy (Cloudflare Worker / Vercel Edge).
# Leave unset to use CSV upload and sample data only.
# Example:
# VITE_PROXY_URL=https://axiomic-proxy.your-account.workers.dev
