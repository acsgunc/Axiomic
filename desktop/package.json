{
  "name": "axiomic-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Axiomic Tauri 2.0 desktop shell (reuses the web frontend).",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
